/*
import {
  find, filter, has, get, set, capitalize, isString,
  isFunction, isArray, isEmpty, clone, remove, isNull, first, last,
  kebabCase, snakeCase
} from 'lodash';

export {
  find, filter, has, get, set, capitalize, isString,
  isFunction, isArray, isEmpty, clone, remove, isNull, first, last,
  kebabCase, snakeCase
};

export {
  find, filter, has, get, set, capitalize, isString,
  isEmpty, isFunction, isNull, isArray,  clone, remove,  first, last,
  kebabCase, snakeCase
} from 'lodash';
*/
